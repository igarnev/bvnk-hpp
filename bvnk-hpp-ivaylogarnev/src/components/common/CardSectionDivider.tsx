import { Separator } from '@components/ui/separator';

export const CardSectionDivider = () => {
  return (
    <div className="px-6">
      <Separator className="bg-gray-200" data-testid="separator" />
    </div>
  );
};
